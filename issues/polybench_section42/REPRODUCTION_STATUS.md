# PolyBench Section 4.2 Reproduction Status

Last updated: 2026-09-05T18:18:00-07:00

Reorganization update: 2026-09-05

- PASS — Rebased the isolated evaluation worktree onto shared `raisetolinalg` commit `e77f674c17d43f1a2bfbf62897e433d14ca74d5e`; Git skipped the already-applied Section 4.2 patch rather than duplicating it.
- PASS — Rebuilt `cgeist` and `polygeist-opt` from that exact clean evaluation worktree using `cmake --build /tmp/polygeist-section42-build --target cgeist polygeist-opt -j4`.
- PASS — Jetson control access has recovered: the device again reports Orin compute capability 8.7, driver 615.06, and CUDA compiler 12.6.85. Previous timed binaries were no longer present, so no old executable was reused.
- PASS — Sweep scripts now accept an optional explicit kernel list, allowing blocked and missing rows to be rerun without overwriting unrelated retained results.
- PASS — Reorganized the viewer around one manifest-driven PolyBench page with exactly four primary runtime columns: native CPU, raised CPU-library, native PolyBenchGPU CUDA, and raised CUDA-library. The old split `polybenchgpu.html` and `polybench-section42.html` outputs are removed, and the hardcoded legacy GPU timing table is no longer a data source.
- PASS — Added and passed a focused viewer regression for the four-column contract and obsolete-page cleanup.
- PASS — Current-branch OpenBLAS correctness revalidation reconfirmed `2mm`, `atax`, `bicg`, `gemm`, `gemver`, `gesummv`, and `mvt`; `doitgen` still fails ABI lowering before execution and `gramschmidt` fails numerically with non-finite output.
- FAIL — Recovered-device raised-GPU rerun of `3mm` completed but returned zero for all 880,000 output values; correctness failed and timing remains withheld.
- FAIL — Recovered-device raised-GPU rerun of `gesummv` aborted with an illegal CUDA memory access; correctness failed and timing remains withheld.
- FAIL — Recovered-device raised-GPU reruns resolved the remaining blocked rows: `gramschmidt` exceeded the device launch timeout and `mvt` aborted with an illegal memory access. No raised-GPU timing is accepted for either row.
- PASS — Cross-compiled canonical native-GPU adapters for `gemm`, `2mm`, `3mm`, `gesummv`, and `gemver` entirely on the x86 host. Each adapter retains the PolyBenchGPU commit `5584aaa7` computational kernel, uses canonical PolyBench/C LARGE dimensions, FP64 initialization and output, and is explicitly marked modified-source because integration and launch geometry were normalized.
- PASS — All five canonical PolyBenchGPU configurations pass the numeric criterion and have one warmup plus five accepted samples. Medians are recorded in `performance_gpu.csv` with separate CUDA-event device and PolyBench-call end-to-end scopes.
- PASS — Current-branch OpenBLAS correctness revalidation completed with the same seven passes (`2mm`, `atax`, `bicg`, `gemm`, `gemver`, `gesummv`, `mvt`) and the same two failures (`doitgen` lowering, `gramschmidt` numeric/non-finite output).
- PASS — Retimed all seven correctness-approved OpenBLAS rows on pinned CPU 21 with one BLAS thread, one warmup, and five samples. Current medians are 66.929 ms (`2mm`), 7.947 ms (`atax`), 7.219 ms (`bicg`), 50.437 ms (`gemm`), 69.042 ms (`gemver`), 3.416 ms (`gesummv`), and 7.934 ms (`mvt`).

## Scope and acceptance rules

- Source under evaluation: Polygeist commit `e77f674c17d43f1a2bfbf62897e433d14ca74d5e` (`raisetolinalg`), isolated in `/home/arjaiswal/Polygeist-section42` on branch `eval/polybench-section42`.
- The shared `/home/arjaiswal/Polygeist` checkout has unrelated uncommitted runtime, matcher, benchmark, and LLVM-submodule edits. They remain excluded from these measurements and are not modified by this work.
- Canonical suite: all 30 PolyBench/C kernels under `tools/cgeist/Test/polybench`, excluding the alternate `Nussinov.orig.c` source.
- A performance datum is accepted only after the same configuration passes output correctness against native C using identical source, dataset dimensions, datatype, and initialization.
- Only external CPU/GPU libraries are eligible for matched-path performance. Project-authored computational kernels and benchmark-specific matcher rules are prohibited.
- GPU device-only and end-to-end times are recorded separately.

## Concrete results and blockers

- PASS — Created isolated evaluation worktree and branch without touching the dirty source checkout.
- PASS — Enumerated 30 canonical PolyBench/C kernels.
- PASS — Host exposes OpenBLAS (`libopenblas.so`) and `aarch64-linux-gnu-gcc`.
- PASS — `pva-general` is reachable with non-interactive SSH.
- PASS — Direct Jetson access through `pva-general` is reachable: Orin (`sm_87`), driver 615.06, CUDA compiler 12.6.85, cuBLAS 12.6.4.1.
- PASS — Fixed the protocol at explicit `LARGE_DATASET`, `DATA_TYPE_IS_DOUBLE`, and the checked-in initialization for all configurations. Source SHA-256 values are recorded for all 30 rows.
- FIXED — The checked-in `run_all_e2e.sh` covered only 26/30 canonical kernels. The isolated branch now includes `adi`, `seidel-2d`, `durbin`, and `ludcmp`; the complete 30-row sweep ran.
- PASS — Clean `cgeist` and `polygeist-opt` build from the isolated worktree; configure/build logs and final binary SHA-256 values are retained in `logs/`.
- FIXED — Commit `c4dda7f5` loops indefinitely in the GEMM raising pipeline. Ported only the generic four-line invariant-hoisting guard from the dirty checkout; a new benchmark-name-independent dynamic GEMM regression completes in 0.01 seconds, and the formerly hanging PolyBench GEMM stage now completes in 0.01 seconds with four Linalg ops.
- BLOCKED — The checked-in matcher round-trip marker restoration produces duplicate `%extracted_slice` SSA definitions for GEMM, so that diagnostic-only output cannot be parsed by `mlir-opt`. Residual correctness is tested directly from raised/debufferized IR, and actual library paths are tested from semantic matcher output.
- PASS — `LARGE`/double exact native-vs-residual correctness: `gemm` (6.59 s total build/check), `2mm` (10.74 s), `3mm` (14.68 s), `atax` (0.91 s), and `bicg` (0.91 s).
- FAIL — `adi` still leaves `polygeist.submap`/`submapInverse` after raising and lower-submap, so it is rejected before CPU lowering (0.46 s). This reproduces a known failure; detailed IR diagnosis is pending.
- PASS — `cholesky` exact native-vs-residual correctness at `LARGE`/double (181 s total build/check).
- FAIL — `correlation` reaches execution but residual output differs from native, beginning with extra diagonal values (`2.00`); rejected after 57.90 s. This reproduces a known correctness failure.
- PASS — Rechecked known failure `covariance`: it now passes exact native-vs-residual correctness at `LARGE`/double (44 s). No performance value is accepted until matcher/library status is separately established.
- PASS — `deriche` (30 s), `doitgen` (15 s), and `fdtd-2d` (20 s) exact native-vs-residual correctness at `LARGE`/double.
- FAIL — `durbin` leaves two dynamic tensor `polygeist.submap` operations and is rejected before CPU lowering (0.41 s). This reproduces a known failure.
- PASS — `floyd-warshall` (102 s), `gemver` (2 s), `gesummv` (1 s), and `gramschmidt` (68 s) exact native-vs-residual correctness at `LARGE`/double.
- UNAVAILABLE — KernelFaRer commit `53a10e6` fails to build against both available LLVM 18 and LLVM 14 APIs. Preceding artifact commit `9d04406` also fails against LLVM 14 (`MatrixBuilder` API). No older compatible LLVM is installed; all three build logs are retained.
- PASS — System LLVM/Clang 14 exposes integrated Polly and accepts a Polly-enabled compilation probe. Common-subset correctness/timing remains pending.
- PASS — `heat-3d` (39 s), `jacobi-1d` (1 s), and `jacobi-2d` (12 s) exact native-vs-residual correctness at `LARGE`/double.
- PASS — Polly 14 exact correctness for common-subset `gemm` at `LARGE`/double.
- REJECTED — Initial GEMM native-Clang14/Polly timing batch overlapped the LU correctness sweep and showed strong contention variability. Those raw samples are retained but are not entered in `performance_cpu.csv`; timing will be repeated serially on an idle host.
- PASS — Completed the 30/30 `LARGE`/double native-vs-residual sweep. Exact comparison passes for 24 kernels. `correlation` is a numeric mismatch; `adi`, `durbin`, `ludcmp`, `nussinov`, and `seidel-2d` are partial lowering failures. All stage IR and error logs are retained.
- PASS — Late sweep results: `lu` (181 s), `mvt` (1 s), `symm` (16 s), `syr2k` (27 s), `syrk` (12 s), `trisolv` (1 s), and `trmm` (18 s).
- FAIL — `ludcmp` retains one dynamic tensor submap; `seidel-2d` retains eight tensor submaps; `nussinov` retains two memref submaps. Each is rejected before executable residual lowering.
- PASS — Matcher sweep completed for all 30 manifest rows. Twelve correctness-passing kernels emit external-library launches; twelve correctness-passing kernels have an explicit `no_match`; the six residual failures are matcher-unavailable. Full launch counts and kinds are in `logs/matcher_sweep_summary.csv`.
- FIXED — Multi-operation GEMM matching originally left later slice/insert users attached to the replaced scale result. The generic composition-root rewire now preserves those uses and has a benchmark-name-independent regression.
- FIXED — Enabling the CBLAS runtime exposed a C `<complex.h>` macro collision with two runtime parameter names. The generic rename is covered by a compile regression and the runtime now builds against system OpenBLAS.
- PASS — Matched GEMM executes through system OpenBLAS/CBLAS and passes the declared numeric criterion: 1,100,000 values, zero failures, maximum absolute error 0.01 and maximum relative error 4.45e-05 (`rtol=5e-4`, `atol=1.1e-2`). Exact comparison is inappropriate for reordered floating-point BLAS reductions; the comparator and close/far regression inputs are retained.
- PASS — Focused regressions for invariant hoisting, composition-root rewiring, and CBLAS runtime compilation pass. The numeric dump comparator also passes its close case and rejects its far case.
- PASS — OpenBLAS/CBLAS correctness passes for eight matched kernels: `2mm`, `atax`, `bicg`, `gemm`, `gemver`, `gesummv`, `gramschmidt`, and `mvt`. Each used the same source, `LARGE` dimensions, FP64 data, initialization, and retained native reference as the residual test.
- FAIL — Matched `doitgen` lowers two runtime calls but standard MLIR lowering rejects changed tensor loop-carried values after ABI lowering. It receives no CPU-library performance result unless repaired.
- UNAVAILABLE — `3mm` is matched only to cuTensorNet, while `covariance` and `deriche` emit only zero-fill launches; none is an OpenBLAS/CBLAS CPU-library configuration. Their CPU-library cells are not to be conflated with the eligible CBLAS sweep.
- PASS — Generic build-driver fixes now allow semantic-MLIR artifact export and initialize the selected stencil backend on the semantic-resume path.
- FAIL — First Jetson execution of matched GEMM builds and runs through real CUDA/cuBLAS but is numerically wrong: 1,098,823 of 1,100,000 values fail, beginning at index 1,100 with a zero result versus 453.66. No GPU timing is accepted. Cross-build, deployment, output, and comparison logs are retained.
- PASS — Full matched Jetson correctness sweep attempted all twelve launch-emitting kernels using host cross-compilation only. `atax`, `bicg`, `covariance`, `deriche`, `gemver`, `gesummv`, and `mvt` pass the numeric criterion through CUDA/cuBLAS or CUDA memset.
- PASS — `3mm` cross-builds against staged aarch64 cuTensorNet/cuTENSOR, deploys to the Jetson, and passes all 880,000 compared FP64 values exactly. The first attempt used an incomplete header root; its log is retained alongside the successful retry.
- FAIL — Jetson `2mm` executes but all 960,000 output values fail (the result is zero); GEMM fails 1,098,823/1,100,000 values; `gramschmidt` fails 428,561/2,640,000 values. No timing is accepted for these rows.
- FAIL — `doitgen` has the same loop-carried tensor ABI-lowering rejection on the GPU build as on CPU. All other correctness-passing residual kernels have no GPU library match and will be recorded explicitly as unavailable rather than timed.
- INVALIDATED — Audit of the first matched CPU/GPU sweep found that a trailing passthrough `-O3` overrode the build driver's intended harness `-O0`. GCC cloned the original weak kernel into `main`, allowing some executions to bypass the matched wrapper. All earlier matched pass/fail outcomes are retained as raw diagnostics but withdrawn; none is accepted. The generic driver now places `-O0 -fno-inline -fno-inline-functions` last, with a regression assertion, and every matched configuration will be rerun.
- PASS — Added a generic optional CUDA-event timing wrapper and focused generation regression. This reports device scope around the complete raised kernel; PolyBench's own timer independently reports end-to-end kernel-call time.
- PASS — Reran every OpenBLAS configuration after preventing harness cloning. The same eight kernels pass through the actual strong matched wrapper; `doitgen` remains an ABI-lowering failure. Six focused regressions, including the harness flag-order assertion, pass.
- PASS — Reran every matched Jetson configuration after preventing harness cloning. True passes are `3mm`, `atax`, `bicg`, `covariance`, `deriche`, `gemver`, `gesummv`, `gramschmidt`, and `mvt`; `3mm` again passes all 880,000 values using cuTensorNet/cuTENSOR.
- FAIL — True matched Jetson failures are `2mm` (960,000/960,000 output failures), `gemm` (1,098,823/1,100,000 failures), and `doitgen` (ABI-lowering rejection). The earlier `gramschmidt` mismatch was an invalidated harness-cloning run; the corrected run is exact.
- INVALIDATED — A second symbol audit found the canonical PolyBench kernels themselves are declared `static`. Without `-Dstatic=`, weakening cannot affect them and `main` still calls a local source kernel even at `-O0`. All matched outcomes stated above remain retained diagnostics but are again withdrawn. The corrected protocol exposes only the benchmark translation unit's kernel with `-Dstatic=`; `polybench.c` is compiled separately with that define filtered out. CPU and GPU matched sweeps are being rerun, and final acceptance additionally requires the linked binary to contain the strong wrapper and no local source-kernel implementation.
- PASS — Under the audited strong-wrapper path, OpenBLAS correctness passes for `2mm`, `atax`, `bicg`, `gemm`, `gemver`, `gesummv`, and `mvt`. `gramschmidt` is a genuine failure (1,598,673/2,640,000 values fail, including zeros, NaNs, and infinities), and `doitgen` remains a lowering failure. The comparator was strengthened to count and reject non-finite tokens, with a regression.
- PARTIAL — Under the audited strong-wrapper Jetson path, `covariance` and `deriche` pass exactly. `2mm`, `atax`, `bicg`, `gemm`, and `gemver` are genuine numeric failures; `doitgen` fails lowering. `gesummv` hung for more than two minutes and was terminated; afterward the Jetson CUDA driver reported the device busy/unavailable even with no compute process listed, so clean reruns of `3mm`, `gesummv`, `gramschmidt`, and `mvt` are externally blocked pending device recovery. No timing from this episode is accepted.
- REJECTED — The first pinned CPU timing attempt was stopped after four `cholesky` samples because its harness initialization was intentionally `-O0`; Cholesky's cubic initializer made each process spend roughly a minute outside the measured region. Those partial values remain raw only. The audited harness now uses `-O3` initialization while disabling inlining and semantic interposition of the exposed weak source kernel; object relocation checks prove `main` still resolves to the strong wrapper.
- REJECTED — Audit of the restarted CPU timing batch found native `deriche` reporting exactly zero: with `POLYBENCH_TIME` alone, Clang eliminated that pure source kernel because the output-print guard is normally false. The entire partial timing batch is retained but withdrawn for consistency. The final timing build defines both `POLYBENCH_TIME` and `POLYBENCH_DUMP_ARRAYS` for every configuration, discards dump text outside the timed region, and thus makes every live-out observably used.
- REJECTED — A second DCE audit found that Clang can still inline and delete native `deriche` even when dump output is enabled, because the resulting values are provably independent of its uninitialized input. Final native builds retain `-O3` inside a separately emitted kernel using `-fno-inline -fno-inline-functions`; every binary must pass a disassembly audit proving `main` calls its kernel. Timed binaries can then use `POLYBENCH_TIME` alone. Earlier DUMP-enabled residual samples remain eligible because their separately compiled wrapper/kernel objects are unchanged and dumping occurs after the timed region; earlier native samples are withdrawn.
- BLOCKED — A recovery probe at 15:43 PDT found no listed compute process but the previously passing covariance executable still aborts with CUDA error “CUDA-capable device(s) is/are busy or unavailable” (exit 134). No Jetson timing or blocked correctness rerun is fabricated; `logs/jetson_recovery_probe_1500.txt` is retained.
- SUPERSEDED — The initial audit correctly rejected unmodified PolyBenchGPU programs because they use independent FP32 sources, fixed dimensions, initialization, and timing scopes. After device recovery, five explicitly modified-source adapters normalized only their integration, dimensions, datatype, inputs, and timing while retaining the external computational kernels; their new results supersede the blanket unavailable status for those five rows.
- PASS — DCE-safe, pinned residual timing has five post-warmup samples for `2mm`, `3mm`, `atax`, `bicg`, `cholesky`, `covariance`, `deriche`, `doitgen`, `fdtd-2d`, `floyd-warshall`, `gemm`, `gemver`, and `gesummv`. The continuation is serial and still in progress; medians are not published until the batch and final native baseline complete.
- PASS — Additional five-sample residual timing completed for `gramschmidt` (70.84–73.99 s), `heat-3d`, `jacobi-1d`, `jacobi-2d`, and `mvt`.
- UNAVAILABLE — The `lu` residual timing warmup was killed by the host before producing a sample; at observation time swap was fully occupied (3.8 GiB) even though 57 GiB RAM was available. Its earlier correctness pass remains valid, but no LU performance value is accepted. The batch continued without inventing a result.
- PASS — Final native C timing completed for all 30 kernels: five post-warmup samples per kernel, Clang 18 `-O3`, CPU 21 pinning, and 30/30 direct-call disassembly audits. The corrected native `deriche` median is 80.851 ms rather than the rejected zero result.
- PASS — Accepted CPU timing ledger contains 64 rows: 30 native, 23 raised residual, seven correctness-approved OpenBLAS/CBLAS, and four correctness-approved Polly 14 rows. Every row has five samples and a median; `lu` is the sole correctness-passing residual without a timing because its warmup was killed by the host.
- PASS — Polly 14 exact output correctness passes for the complete common subset: `gemm`, `syr2k`, `2mm`, and `3mm`. Accepted medians and speedups versus the audited Clang 18 native baseline are in `performance_cpu.csv`.
- UNAVAILABLE — KernelFaRer is explicit for all four common-subset rows: both tested source revisions fail against the installed LLVM APIs, so no correctness or timing result is claimed.
- PASS — Jetson recovery resolved every formerly blocked raised-GPU row to an explicit correctness failure. No raised-GPU timing is accepted: ten eligible configurations fail correctness or lowering, while twenty are unavailable because raising fails or no eligible full computational-library match exists.
- PASS — All 30 manifest rows have explicit statuses in every configuration column; there are zero `PENDING` cells. Current counts are: native CPU 30 pass; raising 25 pass/5 partial; residual 24 pass/1 fail/5 unavailable; raised CPU library 7 pass/2 fail/21 unavailable; raised GPU 10 fail/20 unavailable; canonical native GPU 5 pass/25 unavailable; Polly 4 pass; KernelFaRer 4 unavailable.
- FIXED — The first full four-runtime viewer attempt used the Slack virtual-environment Python, which lacks `egglog`, and was rejected. A retry with `/usr/bin/python3` rendered all kernel families but correctly stopped on a stale Section 4.2 artifact link left by the other worktree. The generated-output directory was preserved as `/tmp/ir_viewer_pre_four_runtime_20260905` before the final clean rebuild.
- PASS — Rebuilt the complete viewer from an empty output directory with `/usr/bin/python3`, verified the single PolyBench landing/navigation entry, exactly four runtime headers, all 30 manifest rows, and absence of the obsolete split pages. The strict link audit checked 7,822 local links across 1,633 HTML pages with zero broken targets. Result URL: `file:///tmp/ir_viewer/polybench.html`.

## Required outputs

- `manifest.csv`: final 30-row explicit per-stage ledger; zero pending cells.
- `performance_cpu.csv`: 64 accepted correctness-gated median timings with scope, library, command, and raw-log paths.
- `performance_gpu.csv`: five correctness-approved canonical PolyBenchGPU records, each with separate device-only and end-to-end timing. Raised-GPU records remain absent because no eligible raised configuration passes correctness.
- `logs/`: retained commands, versions, builds, raw samples, rejected attempts, comparisons, symbol audits, failure reasons, viewer build, and viewer link check.

## Incomplete experiments

The run is closed with explicit outcomes rather than fabricated substitutes. Remaining incomplete experiments are: fixing the ten raised-GPU correctness/lowering failures before timing; canonical PolyBenchGPU adapters for the other 25 kernels where an external CUDA source exists; KernelFaRer on a compatible LLVM toolchain; residual timing for host-killed `lu`; five submap-lowering failures; and the `correlation` numeric failure. The viewer repeats the failure reason for every affected kernel.
